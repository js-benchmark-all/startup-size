Benchmark startup time, minified size and gzipped size.

```sh
# Install all dependencies
bun i

# Build all cases
bun task:build

# Startup benchmark
bun task:start [runtime]
```

Configurations are in [./lib/config.ts](./lib/config.ts).
